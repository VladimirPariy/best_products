import React, { FC } from "react";
import Title from "components/ui/title/title";

const NotFoundPage: FC = () => {
  return <Title>Page not found (error 404)</Title>;
};

export default NotFoundPage;
