import {userRegistrationSlice} from "store/slices/user-registration-slice";

export const {userRegistrationRejected, userRegistrationPending, userRegistrationFulfilled, userRegistrationTrigger} = userRegistrationSlice.actions